PS C:\Users\gonza\OneDrive\Desktop\web proj\venturehub> firebase deploy --only storage

Error: Cannot understand what targets to deploy/serve. No targets in firebase.json match '--only storage'. If you are using PowerShell make sure you place quotes around any comma-separated lists (ex: --only "functions,firestore").
PS C:\Users\gonza\OneDrive\Desktop\web proj\venturehub> 
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to listing cover images
    match /listingCovers/{allPaths=**} {
      allow read: if true;
      // Allow write only for authenticated users (to upload images)
      allow write: if request.auth != null;
    }

    // Default: deny all other access
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
